# YNAB personal access token
# Generate at: https://app.ynab.com/settings/developer
YNAB_API_KEY=

# Default budget: 'last-used' auto-selects the most recently modified budget.
# Or set a specific budget ID (find it via the YNAB web app URL).
YNAB_DEFAULT_BUDGET=last-used
