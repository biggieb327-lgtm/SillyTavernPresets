# 2026-08-10 — error counts were phone era

Archived from `.claude/memory/operational-log.md` on 2026-08-21, verbatim. The row
there is the index entry; this is the full record, including every correction
appended after the fact.

## Failure

**The fleet's two remaining unexplained error numbers — `network` at ~8.7/day on six instances, and 200 capped `unhandled` on cass — both resolved to the Termux phone era. Neither is a live defect.**

## Root cause

`[observed]` nora's retained 200 `network` timestamps split 187 before the 2026-07-26 VPS cutover and 13 after: **26.7/day on the phone, 1.08/day on the VPS**. marcus, created VPS-native on 2026-07-29, runs at 0.93/day — statistically identical to nora's VPS-era rate. The "8.7/day" figure every migrated instance showed was an artifact of averaging a 25×-higher phone era across the migration inside a 200-entry cap; 93.5% of nora's retained entries predate the cutover. `[observed]` cass's 200 `unhandled` span 2026-07-25 03:18–17:57 — 14.6 hours, the day before her own cutover — with tracebacks originating in PTB's `networkloop.py:115 network_retry_loop` and **850** `telegram.error.Conflict` lines in her `errors.log`: a second double-poller storm, same shape as jules's 2026-07-19 pilot. bonnie's 58 and priya's 69 both END on 2026-07-26, the migration date.

## System patch

No code change — there is nothing to fix. The counters were already made honest by v2026-08-10.11 (`Errors (retained)` names its cap and its age) and v2026-08-10.12 (Conflict and Forbidden split out of `unhandled`, so a future poller fight files itself correctly and cannot evict a real crash). What remains is an ops action: prune `error_counts` entries predating the cutover so the six migrated instances stop reporting phone history as current.

## Eval

None owed. The mechanisms that would have surfaced this already shipped today; this incident is what confirmed they were the right ones.

## Next

**A max timestamp cannot distinguish "ongoing" from "stopped but recent", and I used it to kill a correct hypothesis.** I proposed phone-era residue, then withdrew it because the newest entry was today — a reading compatible with BOTH "8.7/day ongoing" and "1/day now, 26.7/day historically". Only the daily histogram discriminated. **C8, on the same question, in the same session that took C8 to seen 7.** Also worth keeping: the fleet has no unexplained crashes and no ongoing network problem, which is the first time that has been true and provable. **Prune run 2026-08-10: the fleet went 2130 -> 203 retained, so 90% of every error number anyone has looked at this month was phone history.** VPS-era rates now read nora 2.3/day, bonnie 2.0, cass 0.8, priya 1.9, marcus 1.5, jules 1.0 — and **emily 5.3/day, 2.3x the next highest**, with `memory_ungrounded`, `api` and `note_ungrounded` dominating her pre-prune breakdown. Emily runs `zai-org/glm-4.7:thinking` rather than glm-5 (owner-confirmed, deliberate), so a different extraction-rejection rate is a plausible and benign explanation — those two counters are guards firing, not failures. **Unconfirmed: nobody has read her post-prune per-category split.** That is the fleet's only remaining unexplained number. **Correction 2026-08-10: the prune did not stick on emily.** The handed-over procedure pruned all six files and then restarted all six, leaving a window in which a still-running bot writes its full in-memory `_error_counts` back over the edit; emily returned at exactly her pre-prune 425. The per-instance rates above were measured at prune time and remain valid as measurements, but the on-disk state does not reflect them. Correct order is stop, prune, start — and re-verify all six, since any of them could have saved state in the same window. **Re-run in that order and verified: fleet holds 205 retained, every instance matching its prune-time figure (bonnie +1 and cass +1 in the interval, which is the ~2/day background rate).** emily is confirmed at 74 (5.3/day) on disk. **Resolved 2026-08-10: it is `network`, not the extraction guards.** Her VPS-era split is network 45 (61%), group_ledger 8, memory_ungrounded 7, api 6, media 4, note_ungrounded 2, memory 2. `_count_error("network")` has exactly one call site (`bot.py:15592`, `on_error`'s NetworkError/TimedOut branch), so these are Telegram-side transients that PTB's `network_retry_loop` retries — logged, counted, not user-visible, and untouched by her non-standard model. **Strip network and she is at 2.07/day, squarely mid-fleet**; the outlier is one category, at 3.0x nora's VPS-era network rate on the same host and IP. `[hypothesis]` traffic volume — more Telegram interactions means more chances to time out — which is unconfirmed and would be circular to argue from error counts alone; **the volume hypothesis is now FALSIFIED, and so is the group one.** Normalised against `token_calibration.n` (cumulative measured LLM calls, introduced v2026-07-26.2 so it spans the same VPS-era window): priya has the MOST calls (513) and the FEWEST network errors (7); bonnie at 428 calls has 10 against emily's 45 at a near-identical 421 — 4.5x on the same traffic. Excluding emily, correlation(calls, network) is **-0.67**. Six instances sit in a 7-13 band across a 192-513 call range, which is the signature of a time-based shared cause, not a traffic-based one. Group membership fails too: marcus is in the same group as emily and sits at 11. **emily is a genuine 4.2x outlier with no explanation yet.** `[decision]` not chased further tonight; the next non-circular step is the exception-class tally from her `[net] transient: <Class>: <msg>` log lines, which has been asked for twice and not yet run. **That lead is now dead too, and so is a third hypothesis.** `errors.log` (never pruned, spans 2026-07-04) shows emily 685 vs bonnie 653 all-time — **phone-era 640 vs 643, ratio 1.00** — so the two were identical on the phone and diverged only after the cutover. Same class on both: `NetworkError: httpx.ReadError`. A blocked event loop produces exactly that, and emily is the only instance with Garmin + WSDOT + civitai — but all of those are off-loop (`_run_health_alert_job` and `rhr_monitor_job` both `await asyncio.to_thread(...)`, all three WSDOT call sites likewise), and an AST scan of every `async def` in bot.py for a blocking call outside `asyncio.to_thread` returns **0** — invariant #8 is clean file-wide. `[decision]` **stop hypothesising.** Three theories (volume, group membership, blocked loop) each died to the first dataset able to test them. Known and bounded: 3.2/day of PTB-retried transients, never user-visible. One cheap discriminator left — whether emily's 45 are spread or bursty — with the stopping rule written down: if spread, this is chronic background and the correct action is to stop looking. **Histogram run 2026-08-11: emily's 45 fall on 12 of 14 days at 2-7/day; bonnie's 10 on 6 days. Spread, not bursty — chronic background, and the rule is honoured: CLOSED.** No user impact, bounded, measurable, cause unidentified and not worth further cost. **Correction:** the earlier claim that "invariant #8 is clean file-wide" was reached with a scan matching only low-level names, and bot.py never calls those from an `async def` — it calls sync wrappers. The conclusion survives, the reason was wrong: `assemble_messages` DOES reach `_embed_text` -> `_get_session`, and what makes it safe is a RUNTIME guard (`triggered_memories` calls `asyncio.get_running_loop()` and skips the embed when on the loop, bot.py ~4361) that no AST scan can see. Caught by break-testing the new `async-blocking` scanner, which reported a confident 0 against an injected real violation. **The predicted answer was wrong** (memory_ungrounded/note_ungrounded were expected to lead at 7 and 2), which is the third wrong prediction this session — and the third caught because it was labelled a hypothesis and tested rather than written up as the finding.

