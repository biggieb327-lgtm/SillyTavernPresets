# Debrief ledger

One row per `session-debrief` run. Exists because **the reach-rate cannot be measured from
inside a session**: `evidence-log.sh` writes to the gitignored `.claude/.runtime/`, and
session transcripts live in an ephemeral container — after a restart there is exactly one,
the current session. The repo is the only durable substrate, so the record has to live here
or not exist.

**What this measures:** commits of real work between recorded debriefs. That is a proxy for
"is a debrief overdue", surfaced by `session-audit.sh` at the start of the next session,
where it is actionable.

**What it deliberately does NOT measure:** whether the debrief was reached for unprompted
or asked for. That distinction matters and is exactly the thing a self-report cannot
establish — the agent whose reliability is in question is not a witness to it. The owner is
the only reliable observer of that, and this file does not pretend otherwise.

**No threshold yet.** One session is one data point, and picking a "debrief every N commits"
rule from it would be the estimate-as-fact trap this repo has already paid for twice.
`session-audit.sh` reports the number without judging it. Set a threshold once there are
enough rows to see a distribution.

## Why there is still only one row (2026-08-21)

The skill, `debrief-check.sh` and this ledger were all built on 2026-08-11, and the single
row below is that session recording itself. **170 commits of real work later, nothing had
run the skill again.** The tooling was never the problem — `debrief-check.sh` works and is
honest about what it cannot check. Nothing invoked it.

The only prompt was a `session-audit.sh` line saying "run `debrief-check.sh` when this
session reaches a stopping point": a request delivered at the **start** of a session about
something to do at the **end**, by which time it has scrolled away or been compacted out.
That hook's own comment argued SessionStart was the only actionable moment. One row in ten
days settles it — a reminder nobody acts on is not actionable, whatever the reasoning.

The ask now lives in **`.claude/hooks/debrief-nudge.sh`**, a Stop hook that fires once per
session when the tree is clean and the session committed something that day. It does not
judge whether the work deserves a debrief; `debrief-check.sh` still owns that and can
answer "not a stopping point yet". Deliberately no commit-count gate — that would be the
invented threshold this file has twice refused, and firing on every session that ships
something is also what produces the rows a real threshold would need.

**Read the next several rows as measuring the nudge, not the ritual.** If they cluster at
one row per working session, the mechanism works and a threshold becomes answerable. If
rows appear and say nothing worth keeping, the gate is too loose and should tighten.

Append with: `bash .claude/tools/debrief-check.sh --record`

| date | head | commits since previous | notes |
|---|---|---|---|
| 2026-08-11 | d3fa2b1 | 28 (first row — counted from 3ecd5db, the session's starting point) | first run of the skill, on the session that produced it; C23 minted, C22 → 7, 4 Minor entries retired |
| 2026-08-21 | 87f6b83 | 174 | **First run reached by `debrief-nudge.sh` rather than by asking** — the hook shipped this session and fired on its own first live stop. Found the session's biggest defect *during* the harvest, not before it: 12 of 15 evals reported PASS on a dead parser. C13 → 9, C14 → 5, 3 Minor entries, 5 evals added (41 → 46). |
| 2026-08-22 | 563ab65 | 13 | Artifact-only session (Fleet Graphs workflow diagrams). All 13 commits belong to the prior session that ran earlier today; this session produced 0 commits, 0 code changes. Nudge fired on per-day commit count, not per-session. No constraints to update, no incidents. verify.sh RED is missing PIL/pytest in container, not a regression. |
| 2026-08-23 | 46c736f | 6 | Shipped `/fire` — the fleet's first **proactive** location alert (Seattle Fire real-time 911, `kzjm-xkqj`, ~5-min feed), after `/crime` + `/dispatch` (pull-only) earlier in the session. The user's "alerts aren't working" was correct-by-design: pull-only + daily-lagged data; the fix was a genuinely near-real-time feed, fire/EMS only (no free real-time police source exists). Step-7 `/code-review` caught **4** real dedup bugs a green 1300-test suite missed (id-less re-alert, away-return backlog dump, unbounded set, dropped-past-limit) — all fixed + regression-pinned. Merge integrated a concurrent `v2026-08-21.4` release off main; CHANGELOG had to be reconstructed (main's preamble was misplaced). 2 Minor entries, 0 new constraints, 0 mechanisms owed. CI on `13d79b2` confirmed `completed \| success`. |
| 2026-08-23 | 9bc5bd9 | 14 | Self-improvement session (machinery-only, no bot.py). Benchmarked the repo against 2026 SOTA on self-improving agents → the one real gap was meta-eval of the learning layer, built as the `MECHANISM REVIEW` startup line + `mechanism-recurrence-surfaced` eval; dated the 6 undated graduation markers and widened it; then closed two guard-integrity gaps (`hook-python-compiles`, `hook-refs-exist`, `shell-scripts-parse` over `tools/`). New `watchlist.md` for sub-threshold items (3 open). Harvest: C18 → 7 (a break-test that went red for the wrong reason, self-caught), 2 Minor near-misses (git-dates-from-a-bulk-import, a non-gap almost listed — both caught by grepping first), 1 oplog row. 4 evals added (46 → 50). The hubris discipline (review-not-verdict) was the session's spine and caught 2 would-be false findings. verify.sh RED locally is missing PIL/pytest in container, not the work; CI on `bce41cd` (all code changes) confirmed `completed \| success`, `9bc5bd9` (docs-only harvest) in progress and expected green — same content the local 50/0 suite passed. |
| 2026-08-24 | 468d090 | 3 | Single-purpose machinery session (no bot.py). Baked the owner's separate-reviewer prompt — judge only against the standard, list every shortfall before saying anything positive — verbatim into all three reviewer agents (adversarial-critic, qa-engineer, character-reviewer), and pinned it with the new `reviewer-stance-present` eval, which decides "reviewer agent" from the frontmatter description verb (review/verif/critiqu) so it reaches reviewer agents not yet written. Break-tested both ways RED (stance stripped; detector matches nothing); full suite green (50 → 51 evals). Harvest: 1 Minor near-miss (backslash in an f-string expression — 3.12-only, caught before running), 0 new constraints, 0 mechanisms owed. verify.sh RED locally is missing python-telegram-bot in the container (import/pytest), not a regression — bot.py untouched and parses clean. Not merged to main: session pinned to `claude/reviewer-agent-prompt-gamqk3` by instruction. CI on 468d090 to confirm by hand. Open: `/code-review` + `/security-review` run inline, not as spawned reviewers, so they don't inherit the stance — extend only if owner wants it. |
| 2026-08-24 | 80ef179 | 31 | Continued the morning's merged selector/immutable-release work with ROADMAP 6.2 (sleep-time compute). Shipped two "what nightly can absorb" slices — proactive-hook pre-draft (`v2026-08-24.6`, `NIGHTLY_PREDRAFT`) and ambient-news refresh (`v2026-08-24.7`, `AMBIENT_PREDRAFT`) — and closed slice 3 (selfie pre-selection) as not-applicable after tracing `build_selfie_prompt` (local `random.choice`, no LLM to move; pre-generating would reintroduce the `v2026-08-01.7` frozen-snapshot bug). **Harvest — 1 Minor slip:** asserted "redistributes an existing call, adds none / net-neutral" across 4 files from design intent *without counting the call sites*, when the old call fired only per-send and the nightly one fires a fixed count under heavy gating (so it's a small net *increase* on quiet days). Caught by `/code-review` (repo-change-control step 7, the exact mandated mechanism) and corrected before merge. Adjacent to C8 but outside its readings/samples scope — a cost-count assertion, not a misread reading — so NOT folded into C8 (still seen 8); kept standalone Minor at seen 1. 0 new constraints, 0 mechanisms owed (already guarded by step-7 review). **What worked:** `/code-review` caught the overclaim plus 3 real slice-2 findings; the boolean-flag census test caught a missing DEFAULTS entry; trace-before-build correctly killed slice 3 (C19/C20 discipline in the positive direction). `theory-guard` fired (false positive on already-pasted output). **Open:** 6.2 item 4 (`/reviewlife`, = 5.9) is a feature not a thin slice — mycelium handoff written with a stopping rule; deploy of `.6`/`.7` to the VPS pending (owner runs `vps-sync.sh`; `/audit` must show `2026-08-24.7`). CI on `80ef179` confirmed `completed | success` by hand (run #1003). |
| 2026-08-25 | d332fc8 | 7 | Two releases across one conversation (3 of the 7 commits belong to the prior session). **5.9 `/reviewlife`** (`v2026-08-24.8`): the nightly `reflect()` pass now drafts one-line living-file additions (life/people/projects) gated per-line via `/reviewlife`, riding the existing JSON call (zero new LLM calls); owner-confirmed per-owner/no-nag/cap-20. **6.1 step-1 instrument** (`v2026-08-24.9`): `/audit` now shows `; N cached` from the usage block (`_usage_cached_tokens`, both provider shapes) — the read-only diagnostic that answers "is prompt caching live for our routes?"; the *answer* still needs a live `/audit` read post-deploy. **Harvest:** C1 → 8 and C5 → 3, both from **stop-hook catches on the final report** (unlabeled VPS `vps-sync` block; an unhedged "does a real day produce a good suggestion?" that was an unverified prediction) — the guards did their job. 1 Minor: both releases shipped a changelog claim that overreached the diff (5.9 "REVIEWLIFE=0 stops the drafting" gated only the enqueue; 6.1 "% of in" wrong for the flat usage shape), both caught by step-7 `/code-review` — 2nd occurrence of the 2026-08-24 cost-overclaim shape, guard (mandatory `/code-review`) already exists. 0 new constraints, 0 mechanisms owed. **What worked:** `/code-review` caught 4 (5.9) + 4 (6.1) real defects a green suite missed; `host-guard`/`theory-guard` caught both report slips; the boolean-flag census forced the REVIEWLIFE DEFAULTS entry. **Open (both need one owner action):** `v2026-08-24.8` and `.9` sit un-deployed on main — owner runs `vps-sync.sh` per instance (`/audit` must show `2026-08-24.9`); the same post-deploy `/audit` readings answer 5.9's real-day done-when AND 6.1's live cache read (stopping rule: persistent `0 cached` closes 6.1 not-applicable, nonzero opens step 2 — do not touch `assemble_messages` until then). CI on `ad9f39c` + `d332fc8` confirmed `completed \| success` by hand (runs #1015, #1017). |
| 2026-08-26 | ded0de8 | 13 | Reasoning-leak session (spans 2026-08-25 work + this 24h-later watch/debrief; ~6 of the 13 commits are other sessions'). Fixed the STEPPED THINKING leak (Emily/Priya) on two layers: `v2026-08-25.1` widened `REASONING_LEAK_GUARD` markers (the guard was fitted to priya's transcript, so Emily's cleaner render scored 2 categories not 3 — added the preset's own `epistemic check\|rule priority` vocabulary, break-tested RED→GREEN), then the `character-reviewer` agent drove a reshape of the `[STEPPED THINKING]` block → prose `[BEFORE WRITING]` in both `preset-stepped.txt` (live layer) and `preset.txt` (fallback). Owner deployed all seven. **Harvest:** C5 → 4 (unhedged "the deeper cause…" claim, caught by `theory-guard.sh` — guard scope confirmed wider than its note claims). 2 Minor: nearly edited only the fallback `preset.txt` the reviewer cited (caught by `edit-cards-and-presets`' PRESET_FILES note — live text is the layer); exact-lock verify venv unbuildable in-container (proxy index lags the lock — `garminconnect`/`numpy` pins absent), near-lock fallback used. 0 new constraints, 0 mechanisms owed. **What worked:** `theory-guard` fired correctly; the `edit-cards-and-presets` PRESET_FILES note prevented shipping a no-op to the fleet; step-workflow break-test proved the new marker. **Open (stopping rule in mycelium):** two owner-only signals still unread — `/errors` `reasoning_leak` count and voice-flatness after the prose reshape; persistent zero + no flatness closes it, flatness = one-commit revert of `preset-stepped.txt`. CI on `a05384c`/`8b96fb4` (runs #1023/#1027) and main head `a409d50` (#1037) confirmed `completed \| success` by hand; `ded0de8` (docs-only harvest) to confirm. |
| 2026-08-27 | 9491d39 | 3 | Second stop of the reasoning-leak arc: Emily leaked a THIRD time, and the `v2026-08-25.1` widened guard missed it because it was a different self-invented scaffold ("Analyze the Input / Character Voice / Goal") sharing no vocabulary with the STEPPED THINKING leak — it scored 1 category. Shipped `v2026-08-27.1`: a STRUCTURAL short-circuit in `_looks_like_reasoning_leak` (>=4 line-anchored markdown bold-colon headers over a 600-char floor → re-roll), vocabulary- and name-independent, so it catches novel scaffolds instead of chasing words. **Harvest:** C8 → 9 — the C8-shaped lesson is that yesterday's "the `[STEPPED THINKING]` block is the source of the reasoning-leak class" (shipped in the changelog) was a true reading (leak echoes preset) used for a causal conclusion it did not discriminate; leak #3, a scaffold the preset never contained, was the alternative hypothesis realized. Reshape wasn't wasted (removed duplication) but was mis-motivated. 0 new constraints, 0 mechanisms owed (structural guard already shipped + tested). **What worked:** `/code-review` (mandatory step 7) caught that a 600-char shape-only rule widens the FP surface for Emily's clinical register → tightened to line-anchored headers + stronger negative tests before merge. **Open — now a MODEL decision, not a guard one:** `/errors` showed Emily on `glm-5:thinking` not the documented `glm-4.7:thinking`; the durable cure (if that model spills CoT into `content`) is switching her model — raised with owner, not made. Deploy of `2026-08-27.1` pending (`/audit` must show it). CI on `1a25e40` confirmed `completed \| success` by hand (run #1043); `9491d39` (docs harvest) to confirm. |
| 2026-08-29 | b946ca5 | 5 | Single-purpose session (no bot.py change). Assessed an external skill (github.com/Terryc21/bug-echo): safe, well-built, but redundant with `fix-the-class` and Swift-shaped — verdict was harvest-the-idea, not adopt. Shipped two things: `test_send_triggered_rerolls_leak_end_to_end` (proves the leak guard fires on the UNPROMPTED user-facing path — `send_triggered`, shared by proactive + cron — which was previously only source-traced; break-tested RED→GREEN through the real `_one_call` seam by threading `leak_guard=False`, reverted by re-edit), and folded bug-echo's one non-redundant idea (validate a search pattern against the pre-fix source before trusting a *clean* sweep) into `fix-the-class`. **Harvest:** C5 → 5 (theory-guard.sh caught the send-path conclusion stated as fact when it was a source trace — verified the checkable facts by command, hedged the rest, then closed it by writing the test that *runs* the path); C15 → 3 (risk-guard.sh blocked a checkout-to-revert reached for as a break-test undo while the tree was dirty — the exact trap, hit while editing the skill that warns of it, working principle #8; zero damage). Decision logged (bug-echo not adopted; idea folded). 1 watchlist item opened: risk-guard matches the `git checkout <dirty-file>` string inside heredoc/quoted commit-message bodies, not just executable positions — a safe over-trigger, named with its graduation trigger. 0 new constraints, 0 mechanisms owed — both guards already existed and both fired correctly. **What worked:** theory-guard and risk-guard both did their job; the delivery-gate did not apply (bot.py untouched). No operational-log row — no system failure; the send paths were found correctly guarded. **Not merged to main:** session pinned to `claude/bug-echo-viability-gbf7l4` by instruction (CLAUDE.md git-workflow honors a session-level branch pin over autonomous-merge). Verify.sh green only with the project venv on PATH (system python3 lacks pytest — env artifact, not a regression): 1399 pytest, 57 evals. CI on b064813 / 61d1081 / b946ca5 to confirm by hand. |
| 2026-08-31 | 0041668 | 6 | Single-purpose session (no bot.py): acted as Context Architect. Delivered a KEEP/MOVE/DELETE/FIX audit + 6-part simplification plan to the owner (chat), then executed only the genuinely-uncoupled slice — archived 2 superseded `HANDOFF-*.md` and 5 dead dated review/audit docs into `archive/` dirs (git mv + READMEs), each verified unreferenced by any hook, eval, skill body, skill-router row, or CLAUDE.md backtick. Eval suite 57/0 before AND after. **Deliberately deferred and reported why:** `routines.md` (backtick-referenced in CLAUDE.md → moving fails `claude-md-refs-resolve`; ships with the rewrite), `constraints.md` split (no clean cut — bulk is active constraints + active Minor log), retiring `termux-device-ops`/`vps-migration` (owner judgment, kept loadable on purpose). CLAUDE.md rewrite left as a reviewable proposal per the branch-pin instruction. **Harvest:** C5 → 6 — classified those three items "low-risk" in the report and offered to execute before checking any against the referential-integrity evals; withdrew on investigation. C5's uncovered general half (a planning-risk label is not a function-return claim, so `theory-guard.sh` cannot see it). 0 new constraints, 0 mechanisms owed. No oplog row — no system failure. **What worked:** the referential-integrity evals (`claude-md-refs-resolve`, `skill-index-integrity`, `skill-refs-resolve`) are exactly what made the coupling visible and forced the honest re-scope; taking a green baseline first caught the risk before any file moved. **Not merged to main:** session pinned to `claude/context-architecture-review-igspsi` by instruction. verify.sh RED locally = `telegram` module absent in the cloud container (import/pytest), bot.py untouched and no `.py` changed. CI on `3fc99c4`/`0041668` to confirm by hand. |
| 2026-08-31 | 2fce4be | 6 | Second debrief of the day (part 1 was 0041668, the context-architecture archival). Covers the Context-Architect follow-through: CLAUDE.md rewrite + a coordinated private→public update across CLAUDE.md, the `no-live-raw-urls` eval, 4 skills and 2 ops docs (repo public again 2026-08-31); a real bot.py release **merged to main** (97db6d8, CI #1066 `success`) retiring `/update`; a public-repo security pass; and broadening the secret-scan eval. **The release, v2026-08-31.2, retires `/update` unconditionally** — going public silently re-armed its in-place bot.py swap (dead only by accident of the private 404), which bypasses the immutable-release deploy. Shipped v.1 behind a `LEGACY_SELF_UPDATE` opt-in flag (to resolve a `/code-review` dead-code finding), then removed the flag at owner request for zero re-enable → v.2. **Harvest:** 1 oplog row (the /update re-arm hazard), 1 decision logged (/update permanently retired, what it beat + why), 1 Minor (`$?` after a pipe reads `tail`, not pip — masked a venv-install failure, self-caught), 1 watchlist recurrence (risk-guard content-blind block hit a 2nd matcher, still sub-threshold). 0 new constraints. **Security:** working tree + all 55 commits of history scanned for every credential shape — CLEAN. **What worked:** `/code-review` caught the dead-code + 409→410; the boolean-flag census caught a missing registration twice; `theory-guard` fired on a behavioral claim and I answered with executed test output; break-test red→green on the widened secret-scan. verify.sh green (1401 pytest, 58 evals, 53/53 corpus); CI #1066 on main `success` confirmed via GitHub. **Open:** owner to run `vps-sync.sh` per instance and confirm `2026-08-31.2` on `/audit`; the seed `decisions.md` "Emily runs glm-4.7" entry now points at a CLAUDE.md section that no longer carries it (owner called that note irrelevant this session) — reconcile if it still matters. |
| 2026-09-01 | 8837103 | 8 | Single-purpose session (no bot.py): built `skillforge/`, a standalone WikiSkill (arXiv:2608.27454) reimplementation — a skill-evolution loop + skill-quality benchmark — after the owner picked the ambitious scope ("benchmark first, then the full loop") over an analysis-doc or a direct `.claude/` graft. Separate project like `voicekit-starter/`; bot rules do not apply; never touches `.claude/` at runtime. Built benchmark-first (deterministic graders + a skill-sensitivity test so the validator provably can fail), then the three-layer workspace, the three agents, and the strict-improvement gate with skills-only rollback + `skill-impact.md` audit trail (Algorithm 1). Offline demo: 0.0→0.5→1.0, early stop, held-out test 1.0; 31 skillforge tests pass; repo 57 evals stay green. **Harvest:** clean session — 0 new constraints, 0 Minor slips, 0 mechanisms owed (no wrong host, no premature done, no theory asserted as fact; the WebSearch summary was replaced by a full read of the uploaded PDF before any building). 1 decision logged (WikiSkill scope: standalone project over `.claude/` graft, with rejected alternatives). 1 mycelium handoff written with a stopping rule (whether to graft a small `skill-impact.md` into `.claude/memory/`). No oplog row — no system failure. **What worked:** benchmark-first ordering + the skill-sensitivity test guarded against the repo's "validator that can't fail" failure mode; running the repo eval suite caught nothing broken (green before and after adding a top-level dir). **Not merged to main:** session pinned to `claude/arxiv-2608-27454-build-b1epe6` by instruction (branch pin honored over autonomous-merge). No PR (not asked). **debrief-check RED, both explained non-defects:** `merged` = the branch pin; `verify` = bot deps absent in the cloud container (`from PIL import Image` fails), bot.py untouched and no `.py` under `telegram-companion-bot/` changed. CI on `16fa608`/`8837103` to confirm by hand. |
| 2026-09-01 | 4c90c8e | 9 | Diagnosis + signed-off plan session (no bot.py; 2 of the 9 commits are the plan doc, the rest carry the harvest + prior sessions'). Root-caused why Emily's `/life` arc read as foreign: the offline-life loop (`_generate_life_event`, `_maybe_rotate_life_arc`) reads only its own `day_*.txt`+prev-arc+projects, never relationship memory, and its rotation carries "unresolved threads" verbatim — so it froze threads (Warren "was it a test", "photo series") that the real relationship already **resolved**; the arc *contradicts* memory. VPS `state.json` dump **disproved the premise that memory is weak** — private chat had a rich 174-word long-term summary + 15 accurate facts; memory is the strong subsystem, the arc is the drifter. Owner signed off a plan (`PLAN-lifearc-memory-grounding.md`, NOT implemented): life READS memory (`get_owner()`→summaries/facts/recent), write firewall untouched, `LIFE_GROUNDING` default-on kill switch, + own-day-trim + near-dup dedup. **Harvest:** C5 → 7 (theory-guard caught me relaying a mycelium `[hypothesis]` — 5.9 real-day suggestion — as fact; identical shape to the 2026-08-25 hit); C8 → 11 (concluded "memory is sparse" from a grep of `memories.txt` alone = one of several stores; self-caught by dumping state); 1 Minor (wrote an "exclude NSFW from grounding" guard without checking it against an NSFW companion — owner's question surfaced it; reframed as content-neutral provenance+domain). 1 decision logged (arc subordinate to memory, with rejected alternatives incl. the NSFW-filter framing). 0 new constraints, 0 mechanisms owed — both guards already exist and C5's fired correctly. **What worked:** theory-guard fired on the relayed hypothesis; dumping the real store (not trusting the grep) inverted a wrong conclusion before it drove the fix. **verify.sh RED locally = missing PIL/telegram deps in the cloud container** (import/pytest), bot.py untouched; the eval suite ran 57/0 (1 skipped = dep-gated). **Not merged to main:** session pinned to `claude/roadmap-priorities-uu9fyp` by instruction — memory-layer harvest + plan sit on the branch; recommend merging so the next session's mycelium/decisions carry them. CI to confirm by hand; no bot.py release this session. |
| 2026-09-01 | a1c6f5d | 11 | |
| 2026-09-02 | 2994149 | 11 | Sprint 2 complete (3 features, 3 releases): 5.5-A chronotype body-clock + user late-night noticing (`v2026-09-01.4`), 5.1-A constancy override "lighthouse" (`v2026-09-02.1`), 5.4-A `/reflect` in-character introspection (`v2026-09-02.2`). All zero/near-zero new LLM calls, pure prompt injection via `assemble_messages`. **Harvest:** 1 Minor (test-pattern confusion — used invented `make_cmd_update` + `@pytest.mark.asyncio` instead of repo's `_cmd_update`/`_cmd_ctx`/`asyncio.run()` pattern; self-caught by reading existing tests before running). 0 new constraints, 0 mechanisms owed. **What worked:** delivery gate enforced version+changelog on all 3 releases; `TestEveryBooleanFlagDefault` lesson applied proactively 3 times (added DEFAULTS entries before running tests); `verify.sh --quick` green on every first attempt (1435→1445→1451 tests). Clean session — no guard failures, no theory slips, no overclaims. |
| 2026-09-02 | 1351a40 | 7 | Sprints 3-5 complete (continuation of the same long session that shipped Sprint 2 at 2994149). 6 releases total today across 2 debriefs: v2026-09-02.3 tip-of-tongue + urgency floor (Sprint 2), v2026-09-02.4 transition marking (Sprint 3), v2026-09-02.5 proactive triage + vigil + bottle (Sprint 4), v2026-09-02.6 comping + trading-fours + cross-query bridge (Sprint 5). All Track 5 sprinted items now shipped. **Harvest:** 2 Minor — (1) wrote a test assertion with wrong logic (`" " not in result or result == result.rsplit(...)`) that is always False for multi-word truncated output; pytest caught it; (2) added 3 `_env_bool` flags without updating the DEFAULTS census table in the same edit; the census test caught it. 0 new constraints, 0 mechanisms owed. No oplog row — no system failure. Skill-impact: the pending reasoning-leak structural guard was not exercised this session (product features, not failure-class work); stays `pending`. Watchlist: 4 open items reviewed, none graduated. No project-changing decisions — all 3 Sprint 5 features followed the existing ROADMAP spec. **What worked:** `TestEveryBooleanFlagDefault` caught the missing census entries (its whole reason for existing); delivery gate enforced version+changelog on all 3 releases; `group-deliver-clean` and `group-cmd-allowlist` evals confirmed Sprint 5's group changes stayed within pinned boundaries; CI green on all commits. **Open:** deploy of v2026-09-02.3 through v2026-09-02.6 via `vps-sync.sh` per instance. |
| 2026-09-03 | 893cddf | 4 | Short session shipping ROADMAP 5.10-B `/mixtape` (v2026-09-03.1, 3 commits, 222 insertions). Pure composition over existing pipelines (milestones + TTS + selfie). 1 Minor recurrence (DEFAULTS table — same class as 2026-09-02, caught by pytest on first run). Theory-guard fired correctly on a behavioral claim ("requires 3+ milestones"); verified by running the code. 0 new constraints, 0 mechanisms owed. CI #1129/#1131/#1133 all green on main. |
| 2026-09-04 | 4a02237 | 13 | Emily restart-loop diagnosis. C1 seen 9, 1 Minor (deploy-verification gap). No new mechanisms. |
| 2026-09-04 | 45b5642 | 10 | Prompt-token reduction across the fleet, ending in a quota incident. Shipped: shared preset dedup (c54eb00, -1,464 tok on every scene instance), Jules Chirp Bank rewritten from Shoresy structural research (e0b7458), Jules protected card fields compressed 5,225→3,837 (108ade9), new `probe-context.py` (26facec, ramp-up rewrite at 157b447), measured fallback window recorded in `.env.example` (8e74cc2). **Harvest:** both slips shared one cause and folded into **C5 (seen 7→9)** rather than minting a constraint — an unverified metering claim written into a docstring as fact, and a whole card pass sized against a 16k window taken from a changelog "e.g." that measurement later put at ~19,859 (Jules fit the entire time; the compression was justified, the urgency was invented). Graduation note re-read: both are external-service properties, not named-function returns, so they sit in C5's acknowledged uncovered half — no guard gap. 1 Minor kept (probe ramp-up design rule, a separate lesson from the assumption behind it). 1 oplog row (weekly-quota exhaustion presenting as a fallback storm, not an outage — bot.py escalates 429 to the fallback model on the same quota, bot.py:7461). 1 skill-impact `pending` (probe + "measure, don't look up"; holds-when and two recurrence shapes recorded). 1 decision logged (context windows are measured, never read from a spec — published figures were off 1.65x and 6.6x). **What worked:** the probe's own UNKNOWN-on-non-size-failure rule refused to convert a 429 into a context number, which is the `hubris` skill working as designed; `handoff-guard` and `host-guard` each caught an unlabelled/relative command block before it reached the owner; `anchor-guard` blocked a line-numbered sed mid-debrief; `shell-semantics-guard` blocked a heredoc-less commit message containing backticks. **Open:** fleet deliberately stopped (quota) — do not restart or `vps-sync.sh` until the owner confirms; three merged content changes await deploy; `FALLBACK_CONTEXT_BUDGET=15700` already set in all seven `.env`s; Euryale unmeasured with a stopping rule (switch only if ≥32,000); `FALLBACK_CONTEXT_BUDGET` is absent from `/audit`, offered as a follow-up. Both open threads are in `mycelium.md`. |
| 2026-09-05 | 496640e | 2 | |
| 2026-09-06 | d38c12f | 5 | Clean single-purpose content session: added ekphrasis composition frame to `preset.txt` (fleet-wide voiceprint) and `TheAtelierV4.json` (longform SillyTavern preset, disabled addon). 1 Minor self-corrected slip (commit syntax blocked by C23 shell-semantics guard — existing constraint, not incremented). 0 new constraints, 0 mechanisms owed, no oplog row. Fleet stopped (NanoGPT quota); deploy via `vps-sync.sh` per instance when restarted. |
