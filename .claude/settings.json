{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "python3 -c \"import py_compile; py_compile.compile('telegram-companion-bot/bot.py', doraise=True)\" 2>&1 && echo '{\"decision\":\"allow\"}' || echo '{\"decision\":\"block\",\"reason\":\"bot.py has a syntax error — fix it before committing.\"}'",
            "timeout": 10,
            "statusMessage": "Checking bot.py syntax..."
          }
        ]
      }
    ]
  }
}
