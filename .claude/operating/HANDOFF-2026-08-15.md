# Handoff — SillyTavernPresets / telegram-companion-bot

**Written 2026-08-15.** A map of the whole system: what it is, every doc, every skill,
every agent, and the machinery that enforces the rules. Supersedes
`.claude/operating/HANDOFF-2026-08-08.md` as the current wide-angle view; that file is
kept for its worked example (§1 there) and is not being deleted.

## How to read this file

This is a **snapshot, and snapshots drift.** The 08-08 handoff was itself proof: a
"quick reference" copy of the skill table drifted once already, omitted seven skills, and
misrouted a session (F1/F2 in `.claude/SCAFFOLDING-AUDIT-2026-07-30.md`). So:

**Authority order when this file disagrees with anything else:**

1. Runtime output (`=== STARTUP AUDIT ===`, `bash .claude/tools/verify.sh`) — always wins.
2. `CLAUDE.md`, `.claude/skills/skill-router/SKILL.md`, the skill bodies themselves.
3. This file.

Treat every number below as "true on 2026-08-15" and re-derive it before relying on it.
The *shapes* — what a skill owns, why a hook exists — age much better than the counts.

**Read order for a first session:** `CLAUDE.md` → `.claude/OPERATING_MANUAL.md` → the
skill `skill-router` points you at. This file is the optional wide-angle view; you do not
need it to do a small task.

---

## 1. Live state at handoff

Verified 2026-08-15 by running the commands, not from memory:

| Thing | Value | How checked |
|---|---|---|
| `BOT_VERSION` | `2026-08-15.1` (`bot.py:100`) | `grep -n '^BOT_VERSION' bot.py` |
| Newest changelog entry | `v2026-08-15.1` — matches | `grep -n '^## v' CHANGELOG.md` |
| `bot.py` | 16,270 lines, compiles clean | `wc -l`, `py_compile` |
| Eval suite | **40 pass, 1 fail** | `bash .claude/tools/verify.sh` |
| The 1 failure | `bot-imports` — **environment artifact, not a code defect** (see below) | traceback read by hand |
| pytest | **could not run** — same environment cause | same run |
| gate_corpus | 53/53 cases behave as specified | same run |
| sweep | 0 candidates across 8 scanners | same run |
| Active constraints | C1–C23 | `.claude/memory/constraints.md` |
| Live Routines | 7 (6 recurring + 1 one-shot) | `.claude/operating/routines.md` |
| Skills | 23 | `.claude/skills/` (excl. `_staging`) |
| Agents | 11 | `.claude/agents/` |
| Hooks | 14 (13 in the 08-08 handoff, +1) | `.claude/settings.json` |

### About the 1 eval failure — read the traceback before reacting

`bot-imports` failed with `pyo3_runtime.PanicException: Python API call failed`, raised
from inside `cryptography`'s Rust bindings, three frames deep in `dist-packages` —
`telegram._payment.stars.transactionpartner` → `telegram._gifts` → ... →
`cryptography.hazmat.bindings._rust`. The traceback never touches `bot.py`. Per the
eval's own header comment, that split — "points into `bot.py`" vs. "points into
`site-packages`" — is exactly how it tells you which case you're in. This container's
`cryptography` package is broken (also blocks `_cffi_backend`, which is why pytest
collection dies on the same panic importing `test_pure.py`). This is the same class of
environment trap the 08-08 handoff recorded for `pytest`/`Pillow` on a fresh container —
not evidence of anything wrong in this repo. `version-changelog-sync`,
`secret-scan`, `bot-compiles`, and every other structural/doc eval passed clean. Confirm
`cryptography` imports cleanly in whatever container you're in before trusting this line
either way; don't assume it's fixed just because this note describes it as environmental.

### What shipped since the 08-08 handoff (82c88fa)

Nine `BOT_VERSION` releases, newest first (`CHANGELOG.md` has full root causes):

- **`v2026-08-15.1`** (today) — `_clean_mes_example()`: the 2026-07-20 Jules fix
  (raw `<START>`/`{{user}}:` labels reaching the model) was patched on her card alone;
  the 2026-08 character pass found the same raw dump still live in Nora, Priya, and
  Marcus. Fixed once in `load_character`, fleet-wide, not per-card. Also closed a
  **fourth** hand-rolled-boolean idiom (`.strip()` without `.lower()`, six flags including
  `MEMORY_AUDIT`, `NOTE_GROUNDED`) that v2026-08-10.9's regex-based sweep didn't match —
  `off` was silently read as ON.
- **`v2026-08-12.1`** — the grounding guard checked a claim's quote was real text, never
  that the claim actually followed from the quote.
- **`v2026-08-10.4` through `.12`** — a nine-release chain the same day: a poller-fight
  incident misfiled as 767 code crashes; `Errors (total)` was neither a total, nor since
  boot, nor bounded; every on/off env var accepted a different word set (the
  hand-rolled-boolean class, later found incomplete — see 08-15.1 above);
  `MAP_INTENT` off fleet-wide with no status surface saying so; three health monitors
  reported running on a bot with no Garmin; a weather 429 became a self-sustaining hot
  loop; Jules got Seattle's weather her whole life on a Portland card; `/place` searched
  the whole country; a slash in a place name 404'd every TomTom lookup; restaurant
  recommendations with no open-hours check.
- **`v2026-08-09.1`/`.2`** — face-lock debug visibility; a month-old coordinate the bot
  could never name a place for.
- **`v2026-08-08.2`** — repair of `v2026-08-08.1` (the release the 08-08 handoff was
  written about while `main` was red): version bump, tag-leak fix, SFW wording restored.

**The error-count investigation that ran through most of 08-10 is CLOSED.** Long story in
the 2026-08-10 operational-log row (surfaced at every session start): the fleet's
error counters were 90% phone-era history retained past a 200-entry cap; after pruning,
`emily`'s one remaining outlier (network errors, 4.2x the next instance) was
histogram-tested and found spread across 12 of 14 days — chronic background PTB-retried
transients, not a live defect, and not worth further cost. Don't re-open it without new
evidence; the stopping rule was written down before the data arrived.

---

## 2. What the product actually is

A fleet of **seven Telegram companion-bot instances**, each an AI character with
persistent memory, running from **one shared `bot.py`**.

Instances differ only by three things: their directory, their `.env`, and their
SillyTavern `chara_card_v2` character card.

| Session | Directory | Card |
|---|---|---|
| `nora` | `/opt/telegram-bots/nora/` | `nora.json` |
| `bonnie` | `/opt/telegram-bots/bonnie/` | `bonnie.json` |
| `cass` | `/opt/telegram-bots/cass/` | `cass.json` |
| `emily` | `/opt/telegram-bots/emily/` | `emily_harper.json` |
| `priya` | `/opt/telegram-bots/priya/` | `priya.json` |
| `jules` | `/opt/telegram-bots/jules/` | `jules_nakagawa.json` |
| `marcus` | `/opt/telegram-bots/marcus/` | `marcus_calder.json` |

All seven run on a **VPS under systemd** as `bot@<instance>` (unit `deploy/bot@.service`,
`WorkingDirectory=/opt/telegram-bots/%i`), sharing one venv at `/opt/telegram-bots/venv/`
and one `bot.py` at `/opt/telegram-bots/bot.py`.

**The authoritative instance list is the set of `bot@<instance>` systemd units**, and the
authoritative instance name is the basename on the runtime `=== STARTUP AUDIT === …
Instance:` line — not the table above.

The phone-era migration is fully complete and its rollback dirs
(`~/<name>-bot.migrated`) passed their 14-day soak on 2026-08-09; they may still exist on
the phone but nothing depends on them anymore.

### Stack

- **Python 3.12** on Ubuntu 24.04. CI pins the same version; the `runtime-version-pinned`
  eval fails if `.github/workflows/evals.yml` and `CLAUDE.md` ever disagree — change both
  together.
- **`python-telegram-bot >=21.0,<22.0`** (async, job-queue). PTB v21's deprecated
  `asyncio.get_event_loop()` call is deliberately worked around in `main()` — don't
  remove it.
- **NanoGPT**, an OpenAI-compatible API at `https://nano-gpt.com/api/v1`.
- **SillyTavern `chara_card_v2`** JSON cards.
- Repo is **private since 2026-07-28** — anonymous `raw.githubusercontent.com` URLs 404.
  Any doc or script still telling you to `curl` one is stale by construction.
- `docs.python-telegram-bot.org` and `nano-gpt.com` are blocked by egress policy to both
  `curl` and `WebFetch` (verified 2026-08-09) — for those two, the version pins in
  `CLAUDE.md` and the code itself are the only sources a session can read.

### Deployment

One command per instance, run **on the VPS as root**:

```bash
/opt/telegram-bots/.repo/telegram-companion-bot/deploy/vps-sync.sh <instance>
```

It fetches and hard-resets the checkout to `origin/main`, copies `preset.txt` + the
instance's preset layers + card + `bot.py` (compile-checked, `bot.py.bak` kept),
normalizes `CHARACTER_CARD`, restarts and enables the unit, then prints hash + STARTUP
AUDIT for verification. **Not curl-piped** — the repo is private.

Claude sessions cannot reach the VPS. You hand the owner commands; they paste output back.

**`/update` is dead as a deploy path.** The handler still exists but downloads over raw
URLs, so it fails `repo_not_readable` and replies telling the owner to run `vps-sync.sh`.
`update-all.sh` and `sync-cards.sh` are historical for the same reason.

**Fleet not yet confirmed on `v2026-08-15.1`** as of this writing — this handoff was
written on `main` immediately after the release; the standard `vps-sync.sh` × 7 pass is
the next action if it hasn't run yet (check `/audit` on each instance for `BOT_VERSION`).

---

## 3. Documentation map — every `.md` and what it's for

### Repo root

| File | What it is |
|---|---|
| `CLAUDE.md` | **The standing instructions.** Project rules, vocabulary discipline, known-deliberate list, git workflow, repo layout, the Notion shared-brain section (new since 08-08). Overrides the operating manual on conflict. |
| `README.md` | Describes **The Atelier**, the root SillyTavern preset — *not* the bot. Root-level presets are a separate archive that the owner still actively edits. |
| `AtelierFieldKitReadme.txt` | Companion notes for `TheAtelierFieldKit.json`. |
| `Chimera_v1_borrow-review_WritersBlock5.md` | A borrow-review of one preset against another. |

### `.claude/` — the operating layer

| File | What it is |
|---|---|
| `OPERATING_MANUAL.md` | **General method, not project rules**: scope the task, decide what evidence is needed, verify claims (executed / read / assumed), report uncertainty in tiers, stop when done. Project rules in `CLAUDE.md` win on conflict. |
| `SCAFFOLDING-AUDIT-2026-07-30.md` | Audit of the agent system itself. F1/F2, both now eval-pinned (`skill-index-integrity`). Read before "improving" the scaffolding. |
| `SESSION-AUTOPSY-2026-08-10.md` | Pattern analysis from a long, high-mistake-rate session; source of C21–C23 and the `hubris` skill. |
| `operating/fable-to-opus.md` | The handoff **before** 08-08 (2026-07-11, refreshed 2026-08-03). Owner-settled decisions and session-earned traps; numbers stale by design. |
| `operating/HANDOFF-2026-08-08.md` | The **previous** wide-angle handoff. Worth reading once for its worked example of a red-`main` incident (§1 there). |
| `operating/routines.md` | The seven live scheduled Routines, with verbatim prompts. **Rule: a live Routine's prompt and this file change in the same session, both directions.** |
| `memory/constraints.md` | Mistakes made *doing the work* (C1–C23), each with a `seen` count. |
| `memory/operational-log.md` | Failures of *the system* — a bot, a deploy, the fleet. One row per incident that changed something. |
| `memory/debrief-log.md` | One row per `session-debrief` run — the durable trace that a debrief happened at all. |
| `memory/improvement-proposals/` | Output of the `improvement-loop-monthly` Routine. |
| `evals/README.md` | The eval discipline: why each check exists, how to add one, the "twice earns an eval" rule. |
| `tools/gate_corpus/README.md` | What a corpus case is and how to run it. |
| `skills/*/SKILL.md` | 23 skills — §4. |
| `agents/*.md` | 11 agent contracts — §5. |

**The distinction between the two memory files is load-bearing.** The test is: *did a bot
misbehave, or did we?* Bot → `operational-log.md`. Us → `constraints.md`.

### `telegram-companion-bot/` — the deployable project

| File | What it is |
|---|---|
| `CHANGELOG.md` | **Read before any `bot.py` change.** Newest first; every entry names the actual root cause. Add an entry after shipping. Skip only for pure content edits. |
| `ROADMAP.md` | What's next and why, evidence + effort + risk + a definition of done per item. Track 4 is the audit backlog. Records the deliberate non-goal: **bot.py stays one file.** |
| `OPS_MANUAL.md` | Day-to-day operation and the **full bot command reference**. |
| `GROUP_CHAT_DESIGN.md` | **Read before touching any `GROUP_*` code.** Survived four adversarial review rounds. |
| `IMPROVEMENTS_PLAN.md` | Release-by-release handoff spec for Track 4. Fully executed — retained as the spec of record. |
| `SETUP_GUIDE.md` | Standing up a new instance. Still marked historical (`<!-- evals: raw-urls-historical -->`); every `curl` in it predates the repo going private. **Still an unclaimed rewrite**, same as 08-08. |
| `GHOST-TOKEN-AUDIT-2026-07-30.md` | Tokens the fleet pays for on every message that add nothing. Reproducible via `tools/ghost_token_audit.py`. |
| `REVIEW-SESSIONMEMORY-2026-07-27.md`, `REVIEW-BRAINENGINE-2026-07-18.md`, `REVIEW-YURALUME-2026-07-18.md` | Owner-requested external reviews of *other* projects, kept for design ideas. Describe nothing in this repo. |
| `CHEATSHEET.md` | Most-used commands. Full reference is `OPS_MANUAL.md`. |
| `AUDIT-2026-07-10.md` | The 2026-07 audit. **Records rejected claims** — check before "fixing" a finding someone already ruled invalid. |
| `README.md` | Public-facing project description and feature list. |
| `deploy/MIGRATION.md` | The phone→VPS runbook. Migration is complete; follow only for reference now. |
| `.env.example` | Every variable `bot.py` reads, documented with defaults. `env-vars-documented` eval enforces completeness. |

### Content files (not docs, but they ship)

- `preset.txt` — **the voiceprint**, the shared texting style feeding all seven bots.
  Editing it changes every bot.
- `preset-{nora,bonnie,cass,emily,priya,jules,marcus}.txt` — per-instance preset layers.
- `preset-{core,closeness,explicit,rp,stepped}.txt` — mode layers.
- `{nora,bonnie,cass,emily,priya,jules,marcus}/` — per-instance seed dirs, each with
  `people.txt`, `projects.txt`, `schedule.txt`, `atlas.txt`.

### Subprojects at the root — none of the bot's rules apply

| Path | What it is |
|---|---|
| `voicekit-starter/` | A standalone author-voice-profiling CLI. Separate project, own rules (`voicekit-work` skill). |
| `idea-scraper-actor/` | A custom Apify actor the monthly Routines call for Reddit + Substack idea scans. **Reddit access still does not work end to end** (open item 3, §10) — needs an Apify Console permission grant out of reach from a session. |
| `character-review/` | The card inbox for the monthly character pass. The Routine reads it and writes proposals; it never edits. |
| `vault/` | A knowledge snapshot built 2026-07-11, pinned to commit `d76dcdf`. An archive, not a source of truth. Excluded from the secret scan. |
| `megumin-mobile/`, `TheAtelier*`, `UnifiedWritersRoom*`, `Chimera*`, `WritersBlock*` | Standalone SillyTavern presets and cards the owner still edits — not frozen. |
| `caa16137-nora.json` | A SillyTavern archive copy that has **genuinely diverged** from the bot's `nora.json` (14 data keys). Never sync them. |
| `index.html`, `weekly-budget.html` | An unrelated personal budget page. |

---

## 4. Skills — all 23

**`.claude/skills/skill-router/SKILL.md` is the routing table. Consult it; don't guess.**

Two new since the 08-08 handoff (which counted 21): **`hubris`** and **`session-debrief`**
— both this file uses to write and verify itself.

Only each skill's one-line description reaches a session for free; every body must be
explicitly loaded, including the two that apply most often (`repo-validation-gate`,
`artifact-first-delivery`).

### Always-consider

| Skill | Load when | What it owns |
|---|---|---|
| `skill-router` | Choosing a skill | The routing table. New skills must be registered here in the same change. |
| `repo-validation-gate` | Before claiming **anything** done | Compile/parse, behavior, regression — evidence for "done." |
| `artifact-first-delivery` | Before deciding where any output goes | Work isn't delivered until pushed. Test: which file enforces this tomorrow? |
| `hubris` | Before writing anything that renders a verdict — a checker, a finding, an explanation for a failure you haven't read the source of | Stops "I could not determine this" from being reported as "this is wrong." Encodes a session where that collapse shipped four times in one tool. |

### Shipping `bot.py`

| Skill | Load when | What it owns |
|---|---|---|
| `repo-change-control` | **Before editing `bot.py`** for anything meant to reach the fleet | The end-to-end release procedure. |
| `bot-code-invariants` | Writing **or reviewing** any `bot.py` diff | Concurrency, output choke point, LLM-call budget, memory provenance, kill-switch policy (#16). |
| `ship` | Work is finished and needs to become a release | Sequencing only, orchestrates the two above plus `deploy-and-verify-fleet`. |
| `deploy-and-verify-fleet` | Work is on main; needs to reach the bots | Exact `vps-sync.sh` commands and failure modes. |
| `fix-the-class` | A fix is about to ship, or a check is being written | Find every other place the same bug shape occurs — proven necessary four times over on 2026-07-25, and again 2026-08-15 (the mes_example fix). |

`repo-change-control` and `bot-code-invariants` **ship together** for any `bot.py` change.

### Debugging and verifying

| Skill | Load when | What it owns |
|---|---|---|
| `repo-debugging-playbook` | A live bot is silent, restarting, crashing, or misbehaving — before proposing any fix | Evidence-first triage order. |
| `termux-device-ops` | The cause looks device-level, or you're touching on-device scripts | Largely historical since the VPS cutover. |
| `verify-external-audit` | A batch of claimed defects arrives from outside — before fixing any | Verify-then-fix. |
| `add-regression-eval` | The same failure class happened twice | How to write **and break-test** an eval without destroying uncommitted work. |
| `session-debrief` | A session hits a real stopping point | Harvest lessons into constraints/operational-log, decide what earns a mechanism, leave a repo-durable handoff. |

### Content and config

| Skill | Load when | What it owns |
|---|---|---|
| `edit-cards-and-presets` | Any content/personality edit | Per-character canon, authoritative on all of it. |
| `bot-config-reference` | Editing an `.env`, choosing a model, debugging a config-shaped failure | Model slots, timeouts, per-instance integrations. |
| `group-chat-changes` | Touching **any** `GROUP_*` code, the ledger, or any handler branching on `chat_id < 0` | The trap: `handle_message` is also the group entry point. |

### Other projects and modes

| Skill | Load when | What it owns |
|---|---|---|
| `voicekit-work` | Any change inside `voicekit-starter/` | Separate project, own rules. |
| `vps-migration` | Reference on the phone→VPS move | Mostly historical now — migration complete. |
| `unattended-loops` | **Before** writing the prompt for a Routine, `/loop`, or overnight run | Frontload every stall point; split live state from history. |
| `grilling` | Stress-testing a plan before building | Interview one question at a time, explore the codebase before asking. |
| `caveman` | Terse-reply mode | ~65% output-token cut. |
| `ponytail` | Minimal-solution / YAGNI mode | Forces the smallest thing that works. See C19/C20 — read-and-agreed-with is not the same as checked-against-the-diff. |

### Skills that do not exist yet

`.claude/skills/_staging/` holds only a `README.md`. A twice-recurring need with no skill
is a finding for `improvement-analyst` and a job for `system-fixer`.

---

## 5. Agents — all 11

Unchanged since 08-08. Subagents are **pre-authorized by owner standing grant**
(`CLAUDE.md` working principle #6) — breadth or multiple parts counts as the user having
asked, in this repo specifically.

| Agent | Model | Mission | Hard scope limit |
|---|---|---|---|
| `chief-operator` | opus | Top-level orchestrator for multi-step work. | Delegates implementation, keeps decisions. |
| `builder` | sonnet | Implement exactly one well-specified microtask with evidence. | Ambiguity or out-of-scope → stop and report. |
| `coder` | sonnet | Code-only: write, edit, debug code and tests. | No research, deploys, docs prose, delegation, GitHub actions. |
| `qa-engineer` | sonnet | Independently verify a claimed-complete change; PASS/FAIL per criterion. | Verification only, fixes nothing. |
| `adversarial-critic` | opus | Attack the work product; find where it's less real than it claims. | Critique only. |
| `character-reviewer` | opus | Judge whether content reads as the character; triage voice defects. | Loads `edit-cards-and-presets` first. |
| `eval-designer` | opus | Turn one observed failure into a permanent runnable check. | `.claude/evals/**` only. |
| `system-fixer` | sonnet | Surgical repairs to the operating machinery. | `.claude/**` only. |
| `improvement-analyst` | opus | Find the highest-leverage system patch from recurring friction. | Analysis + one proposal. Deletions count. |
| `context-librarian` | haiku | Keep the knowledge layer short, current, true. | `.claude/memory/**`, `CLAUDE.md`, changelog formatting only. |
| `research-scout` | haiku | Answer one factual question from primary sources. | Read-only, changes no files. |

---

## 6. The enforcement machinery

### 6.1 Hooks — `.claude/hooks/`, wired in `.claude/settings.json` — now 14

One new since 08-08: **`shell-semantics-guard.sh`** on `PreToolUse` (Bash), born from
**C23** — "the shell evaluated something the command text does not show." Three hooks
still split shell wrapper / Python implementation (`host-guard`, `handoff-guard`,
`claim-guard`).

| Event | Hook | What it does |
|---|---|---|
| SessionStart | `session-audit.sh` | Injects branch, uncommitted count, last operational-log row, repeat-mistake constraint list. |
| UserPromptSubmit | `agent-authorization.py` | Restores the owner's standing subagent authorization. |
| PreToolUse (Bash) | `risk-guard.sh` | Blocks a short, precise list of high-blast-radius commands. |
| PreToolUse (Bash) | `anchor-guard.sh` | C7 — blocks in-place edits addressed by line number against a version-controlled file. |
| PreToolUse (Bash) | `shell-semantics-guard.sh` | **New** — C23's mechanical half. |
| PostToolUse | `evidence-log.sh` | One line per tool call to a per-day evidence log. |
| PostToolUse | `budget-governor.sh` | Counts tool calls; nudges at thresholds. |
| PostToolUse | `py-syntax-guard.sh` | Catches a Python file that stopped parsing this turn. |
| Stop | `host-guard.sh` | C1 — confirm the host before a host-specific command. |
| Stop | `handoff-guard.sh` | C1 family — C16: a handed-over command block must work on someone else's machine. |
| Stop | `claim-guard.sh` | C8 family — catches claiming two artifacts identical on metadata rather than a hash. |
| Stop | `delivery-gate.sh` | The "no fake done" gate: `BOT_VERSION` bump, changelog entry, compile evidence, a test that *calls* any touched `*_cmd`. |
| Stop | `eval-gate.sh` | Bounded retry loop if uncommitted work breaks evals or pytest. |
| PreCompact | `handoff-writer.sh` | Writes a handoff snapshot before compaction. |

### 6.2 Evals — `.claude/evals/run-evals.sh` — 41 checks

Same discipline as 08-08 (fast, dependency-free, run every session and in CI). New checks
since then include `verify-steps-covered` and `skill-refs-resolve`. `SKIP` remains a
distinct outcome from PASS/FAIL for environment-unavailable checks; `bot-imports` today
is a `FAIL` rather than a `SKIP` because the failure is a broken package rather than an
absent one — worth fixing in `run-evals.sh` categorization if this recurs, not fixing in
`bot.py`.

```bash
bash .claude/evals/run-evals.sh   # exit 0 = all PASS
```

### 6.3 Tools — `.claude/tools/`

Unchanged shape from 08-08: `verify.sh` (the standing verification block, compile →
pytest → evals → gate corpus → advisory sweep — `--quick` drops the sweep and is not
enough for a release), `sweep.py` (8 scanners now, up from 7 — advisory, never fails
`verify.sh`), `gate_corpus/` (53 cases now, up from 45 — the guards, guarded),
`subagent-decline-scan.py`, `debrief-check.sh` (new — the session-debrief skill's
mechanical stopping-point check).

```bash
bash .claude/tools/verify.sh
```

### 6.4 CI — `.github/workflows/evals.yml`

Unchanged: same evals + pytest on `main`/`claude/**`, Python 3.12, on every push and PR.
`vps-sync.sh` hard-resets to `origin/main`, so **main must never hold a tree that fails
these.** Open item 1 (§10) still applies: this is a Stop-hook world for Claude sessions,
and CI is the only backstop for anyone else pushing directly.

---

## 7. Memory layer

### `constraints.md` — now C1–C23

Three new numbered constraints since 08-08, all born from `SESSION-AUTOPSY-2026-08-10.md`:

- **C21** — Output engineered to be unfalsifiable is not safe output, it is empty output.
  (seen: 2, 2026-08-10 — bonnie's atlas, then cass's.)
- **C22** — A copy of the thing was consulted where the thing itself was one command away.
  (seen: 7 — promoted from Minor, two occurrences landed *after* it was minted, same
  session.)
- **C23** — The shell evaluated something the command text does not show. (seen: 3 —
  promoted from Minor; now has a mechanical guard, `shell-semantics-guard.sh`.)

The repeat offenders surfaced at every session start haven't changed in kind — C1 (seen:
7, confirm the host), C2 (name the class), C3 (prove RED before GREEN), C7/C17 (anchor on
content, count matches first), C8 (ask what a reading measures), C12/C13 (a command from
docs is a claim about the past; a check that can't fail isn't verification), C14/C15/C18
— plus the new C19–C23. Read the constraint file, don't rely on this summary for the
full text.

### `operational-log.md` — the system failing

Format unchanged: **Date | failure | root cause | system patch | eval | next.** The
newest closed thread worth knowing before anyone re-opens it: **emily's network-error
outlier, closed 2026-08-11** — three hypotheses (traffic volume, group membership,
blocked event loop) each died to the first dataset able to test them; the final
discriminator (spread vs. bursty) came back spread, so it's chronic background and the
written stopping rule was honored. Don't re-chase it without a new symptom.

### `debrief-log.md` — new since 08-08

One row so far (2026-08-11, `d3fa2b1`). Records when `session-debrief` ran; no threshold
for "overdue" yet — one data point isn't a distribution. `session-audit.sh` reports the
commit count since the last row at every session start; **160 commits since the last
recorded debrief as of this session's start** — the SessionStart hook flagged it
explicitly. Worth running `session-debrief` soon; not required to finish this task.

### `routines.md` — now 7 live (up from 4)

| Routine | Schedule (UTC) | Notes |
|---|---|---|
| `ops-brief-daily` | daily | |
| `hygiene-check-weekly` | weekly | |
| `improvement-loop-monthly` | monthly, 1st | |
| `character-pass-monthly` | monthly, 15th | |
| `morning-todo` | daily, 13:45 UTC | **New 2026-08-15**, owner-requested — fires 15 min before `ops-brief-daily`. |
| `practice-scan-weekly` | weekly | **New since 08-08.** |
| `map-fire-rate-review-2026-08-17` | one-shot, `run_once_at` 2026-08-17T16:00Z | Self-disables after firing once. |

Plus one **retired** Routine, kept in the file's history rather than deleted (the
undocumented monthly-loop predecessor, deleted 2026-07-20 — an example of the exact
"exists only in the scheduler or only in prose" drift the rule below exists to prevent).

**Rule unchanged: a change to a live Routine's prompt is mirrored in this file in the
same session, and vice versa.**

---

## 8. Shared brain (Notion) — new since 08-08

The **Fleet Knowledge Base** in Notion (database `89c9e767576149a480221c10d7a97f47`,
data-source `2e75cb5e-bf93-4a2a-a1b8-9d7a1b415e4f`) is a new cross-session memory layer,
added since the 08-08 handoff. It holds findings, decisions, open questions, and
follow-ups that sessions need without re-deriving them from the operational log or commit
history.

- **Before non-trivial work:** search it for `Status=current` entries relevant to the task.
- **During work:** write back findings, decisions, or state changes as rows (Category,
  Status, Source, Tags).
- **Resolving:** update `Status` to `resolved`/`superseded`, never delete rows.

`.claude/memory/` remains the system of record for reviewed, durable knowledge. Notion is
the faster-moving layer that doesn't require a commit to persist — treat it as a
*supplement*, not a replacement: nothing in Notion overrides a repo file on conflict, by
the same authority order as §"How to read this file" above.

---

## 9. How to actually do work here

### Any `bot.py` change

1. Read `CHANGELOG.md`.
2. Load `repo-change-control` **and** `bot-code-invariants`.
3. If it touches `GROUP_*`, `chat_id < 0`, or `handle_message` — load `group-chat-changes` first.
4. Make the change. Bump `BOT_VERSION`. Add a changelog entry, root cause first.
5. Add or extend a test that *calls* any `*_cmd` the diff touches.
6. Run `bash .claude/tools/verify.sh`.
7. Apply `fix-the-class`: where else does this bug shape occur? (This is not rhetorical —
   the 08-15.1 release exists because a "one card" fix from 2026-07-20 turned out to be
   three more cards' worth of the same defect, unfound for three weeks.)
8. Merge to main by **pushing the branch ref**: `git push origin <branch>:main`.
9. Hand off the deploy per `deploy-and-verify-fleet`.

### Any content change (cards, seeds, presets)

Load `edit-cards-and-presets`. `preset.txt` changes all seven bots; `caa16137-nora.json`
must never be synced with `nora.json`. No changelog/`BOT_VERSION` needed for pure content.

### A live bot misbehaving

Load `repo-debugging-playbook` before proposing any fix. You cannot reach the VPS —
every observation arrives because the owner ran something and pasted it back.

### Merging to main — the trap

```bash
git push origin <branch>:main
```

**Do not** `git checkout main` and merge there — a cloud session's local `main` can be
stale with no merge-base against `origin/main` (C13). Always check `origin/main`.

### Owner policy on merging

Merge task branches to `main` autonomously once the full verification block is green.
Any red = stop and report.

---

## 10. Open items (carried and updated from 08-08)

1. **Nothing binds a non-Claude author to this repo's shipping rules.** Still true, still
   unresolved. The delivery gate and eval gate are Stop hooks that only run inside a
   Claude Code session; the `82c88fa` incident that motivated the 08-08 handoff is the
   worked example. Branch protection requiring the `evals` CI workflow green before merge
   is the only mechanism that would bind every author equally. **Needs an owner decision**
   — nothing in this session's evidence suggests it's been made yet.
2. **SFW signalling on the `[clothing:]` override path.** Unresolved since 08-08 — whether
   "Keep the overall image tasteful" survives Gemini's safety filter as well as the
   pre-refactor "Fully clothed, SFW." cannot be settled from a test run; needs a live
   check against the image provider.
3. **`idea-scraper-actor` Reddit access still does not work end to end.** Needs the
   Apify account's Proxy permission granted via Apify Console — not self-servable from an
   API token, out of reach from a session. Substack's path is code-reachable but
   untested — no `substack_publications` URL has been exercised yet as of this writing.
4. **`SETUP_GUIDE.md` is still marked historical, still not rewritten.** Every `curl` in
   it predates the repo going private. A VPS-era rewrite is unclaimed work.
5. **`session-debrief` is overdue by the session's own count** — 160 commits since the
   one recorded row (2026-08-11). Not urgent, but the next session that reaches a real
   stopping point should run it rather than let the gap widen further.
6. **This container's `cryptography`/pyo3 bindings are broken**, blocking `bot-imports`
   and all of pytest. If this recurs in a future session, it is an environment fact to
   report, not a repo defect to chase — confirm with a fresh `pip install` or container
   before assuming either way.

Item from 08-08 now **resolved and dropped**: the phone rollback dirs' 14-day soak ended
2026-08-09 and is no longer load-bearing (§2 above already reflects this in `CLAUDE.md`).

---

## 11. What I'd do first in your position

`main` is green as of `v2026-08-15.1` on every structural/doc check; the one eval failure
is this container's broken `cryptography` package, not the code. If the fleet hasn't been
synced to `.1` yet, that's the immediate action — seven `vps-sync.sh` invocations per
`deploy-and-verify-fleet`.

After that, two things are worth attention rather than more code: open item 1 (branch
protection) is the same owner decision the 08-08 handoff surfaced and it's still open a
week later — worth actually asking rather than re-discovering. And `session-debrief` is
genuinely overdue; 160 commits of lessons are sitting in git history uncompressed into
the memory layer, which is exactly the situation that skill exists to prevent.

Then read `CLAUDE.md` properly, and this file's §1 delta before assuming any number here
is still current in a week.
