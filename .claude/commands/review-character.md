Run a character review pass on one or all bot characters.

Spawn the `character-reviewer` agent in proactive-review mode (Mode B).
The character name is the first argument; omit it to review all seven.

Load `edit-cards-and-presets` first for per-character canon. Output is a
proposal document in `character-review/`, never direct card edits.
