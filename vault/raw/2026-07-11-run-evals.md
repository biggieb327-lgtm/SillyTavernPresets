# Raw capture: eval suite

Source: `.claude/evals/run-evals.sh` @ commit `d76dcdf`. 14 checks, each pinning a
real incident (verified 14/14 PASS this date after `pip install -r
telegram-companion-bot/requirements.txt pytest` + `pip install --upgrade
cryptography` in a fresh container).

Check names: streaming-error-body, heartbeat-alive, graceful-shutdown,
venv-explicit-python, tzdata-pinned, secret-scan, version-changelog-sync,
cards-valid-json, bot-compiles, bot-imports, shell-scripts-parse,
settings-valid-json, group-deliver-clean, group-cmd-allowlist.

Verbatim (group boundary):

> # (a) _group_deliver must stay allowlist-BUILT: none of the DM tail's side effects
> # may appear in its body — those write per-instance flat files that leak into
> # private DMs.

Banned calls in `_group_deliver`: post_reply_analysis, _check_joke_used,
send_selfie, send_meme, _send_voice_reply, _append_user_note, _append_memory.

> # (b) The group command allowlist must stay exactly {"chatid"} — widening it is a
> # reviewed act (edit this eval in the same commit), never a drive-by.

CI (`.github/workflows/evals.yml`) runs the same suite + pytest on every push to
main/claude/**, Python 3.13, 5-minute timeout.
