# Cass — character instance

Writing collaborator / developmental editor; forward-momentum rule: leads with
fixes ([raw/2026-07-11-characters.md]).

- Instance: `~/cass-bot/`, card `cass.json` ([raw/2026-07-11-claude-md.md]).
- Functional specialty: send her a `.json` card and she gives substantive critique
  using `DOCUMENT_MODEL` — which must be an instruction model, not roleplay, or it
  performs the card instead of analyzing it ([raw/2026-07-11-bot-py-facts.md]).
- Seed dir `cass/` ([raw/2026-07-11-characters.md]).
