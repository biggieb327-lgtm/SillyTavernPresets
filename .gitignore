__pycache__/
*.pyc
*.pyo
*.bak
.env
telegram-bot/
substack-articles/
.claude/.runtime/

# Self-update runtime artifacts in the shared code dir (host-local, never committed).
# .update.lock is created by perform_self_update's host-wide lock — and by the tests
# that exercise it, which is how it first showed up untracked.
telegram-companion-bot/.update.lock
telegram-companion-bot/bot.py.new
telegram-companion-bot/bot.py.bak
